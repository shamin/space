import React from 'react'
import styled from 'styled-components'

const Styles = styled.h1`
  padding: 0;
  margin: 0;
  font-size: 5rem;
`

const Logo = ()=>(
  <Styles>💅</Styles>
)

export default Logo
